// Drizzle schema barrel. Each table is one file under this directory (kebab-case)
// with co-located inferred types, re-exported from here. No tables exist yet in
// the bootstrap scaffold; the first tables land in the data-model phase.
export {};
